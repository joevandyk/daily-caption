= stylesheet_link_tag "canvas"
%fb:subtitle
  %fb:action{:href=>"#{update_profile_url(user)}"} Refresh
  %fb:action{:href=>"#{index_url}"} Profile Settings
  %fb:action{:href=>"#{new_caption_url}"} Add New Caption

- fb_wide do
  %h2 Most Recent Caption
  - if user.most_recent_caption
    = render :partial => "contests/photo_caption", :locals => {:photo => user.most_recent_caption.photo, :caption => user.most_recent_caption}
  
  
- fb_narrow do
  == Narrow FB profile for #{name user}